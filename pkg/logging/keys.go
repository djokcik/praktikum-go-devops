package logging

const (
	// TraceIDKey defines the logging key for tracking the Trace ID.
	TraceIDKey = "trace_id"

	// ServiceKey defines the logging key for tracking the service.
	ServiceKey = "service"

	// MetricType defines the logging key for tracking the Order ID.
	MetricType = "metric_type"

	// MetricName defines the logging key for tracking the Barcode.
	MetricName = "metric_name"
)
